<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/css/landing.css">
    <title>Landing</title>
</head>
  <body>
    <div class="login-container">
        <div class="logo">I am a...</div>

        <% if (user && (user.role === 'manager' || user.role === 'cashier')) { %>
          <a href="/manager" class="btn-google">
            Manager
          </a>
        <% } %>

        <% if (user && (user.role === 'manager' || user.role === 'cashier')) { %>
          <a href="/cashier" class="btn-google">
            Cashier
          </a>
        <% } %>

        <a href="/customer" class="btn-google">
            Customer
        </a>
        
        <a href="/menuboard" target="_blank" class="btn-google btn-menuboard">
            📋 View Menu Board
        </a>
        
    </div>
    <button id="translateBtn" data-lang="es"><span>🇪🇸</span> <span>Translate to Spanish</span></button>
    <script src="/js/translator.js"></script>
  </body>
</html>
